let defaults = {
    signedIn: false,
    authHeader: '',
    sessionId: '',
    sessionKey: '',
}

export default class Session {

    constructor(options) {
        if (options != null) {

        } else {

        }
    }
}
