
import Actor from 'client/domain/Actor.js';

describe('Actor', () => {
  it('Has expected default values', () => {
    let defaultActor = new Actor();

    expect(true)
      .to.equal(true)
  })
})
